{
  "name": "cmux-msg",
  "version": "0.17.0",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "bun build src/cli.ts --outfile bin/cmux-msg-bin --target bun --compile",
    "dev": "bun run src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
