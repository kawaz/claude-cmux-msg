{
  "name": "cmux-msg",
  "version": "0.5.1",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "bun build src/cli.ts --outfile bin/cmux-msg --target bun --compile",
    "dev": "bun run src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
